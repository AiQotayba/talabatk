---
alwaysApply: true
---
# 🚀 **React Native Component Architecture – Cursor Rules Version**

## **1) Home Folder: `/components`**

هذا المجلد يحتوي فقط على جميع مكونات الواجهة الخاصة بالتطبيق، مقسّم إلى 5 مجلدات واضحة.

```
/components
  /ui
    /Button.tsx
    /Input.tsx
    /Select.tsx
    /Textarea.tsx
    /Checkbox.tsx
    /Radio.tsx
    /Switch.tsx
    /Slider.tsx
    /Range.tsx
    /DatePicker.tsx
    /TimePicker.tsx
    ...
  /Features
    /Order
    /Address
    ...
  /Layout
  /Atoms
  ...
/constants
  URL.ts
```

---

# 2) **UI/**

مكونات الواجهة العامة التي تُستخدم في كامل المشروع.
تشبه نموذج shadcn UI: مكونات واضحة، بسيطة، وقابلة لإعادة الاستخدام.

### القواعد:

- كل Component شامل وقابل للتهيئة عبر Props.
- لا ننشئ 5 ملفات لكل نوع Input → نستخدم **variants** داخل نفس الملف.
- الهدف: **أقل عدد ملفات + أعلى إعادة استخدام**.

---

# 3) **Features/**

مكونات تخص _ميزة معينة فقط_.
هذه ليست عامة، ولا تُستخدم في التطبيق كله.

### القواعد:

- كل ميزة لها مجلد داخل Features.
- التكرار بين الـ Features يُستخرج إلى ui/ أو atoms/.

---

# 4) **Atoms/**

مكونات صغيرة عامة لا تصنف تحت Features او UI ولكن تستخدم في ميزات متعددة.

```
/atoms
  /Error.tsx
  /Loading.tsx
  /Empty.tsx
  /Retry.tsx
  /QueryStateWrapper.tsx
  ...
```

---

# 5) **Layout/**

مكونات خاصة بالبناء العام للصفحات والمشروع.

### القواعد:

- أي شيء يُستخدم لبناء layout وليس جزء UI مستقل ⇒ مكانه هنا.
- الهدف: كل Screen يكون نظيف وواضح.

# 7) **General Rules (خلاصة تنظيم المشروع)**

### ■ Rule 1 — قليل ملفات، كثير وظائف

الملف الواحد مسموح يصل **500–900 سطر** طالما واضح ومنظم.

### ■ Rule 2 — Variants بدل تعدد الملفات

```
<Input variant="password" />
<Input variant="phone" />
```

### ■ Rule 4 — React Query = Data Layer

بدون Hooks إضافية ويجب استخدام React Query للاستفادة من الميزات الموجودة فيه بدون اللجواء الى data layer متعدد الملفات + Api Client

### ■ Rule 5 — UI = قابل لإعادة الاستخدام

Features = قابل لإعادة التخصيص
Atoms = معالجة الحالة المشتركة
Layout = بناء الصفحات او مكونات للثيم
ui = مكونات للواجهة العامة والمشروع

### ■ Rule 6 — ترتيب داخل كل ملف

1. imports
2. types
3. constants
4. logic
5. component
6. export

### ■ Rule 8 — Naming ثابت

- UI → PascalCase
- Features → PascalCase
- Atoms → PascalCase
- Layout → PascalCase
